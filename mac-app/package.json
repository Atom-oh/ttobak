{
  "name": "ttobak-mac",
  "version": "0.1.0",
  "private": true,
  "description": "TTOBAK Mac App — Tauri 2 wrapper around the TTOBAK SPA with native system-audio capture (ScreenCaptureKit).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "sign": "bash scripts/sign.sh",
    "build:signed": "tauri build && bash scripts/sign.sh",
    "build:signed:debug": "tauri build --debug && bash scripts/sign.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
