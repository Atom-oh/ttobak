{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for TTOBAK Mac App: only core invoke for recording commands.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
