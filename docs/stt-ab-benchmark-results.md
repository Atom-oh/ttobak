# STT A/B Benchmark Results

Date: 2026-04-22 23:52

| Engine | Mode | Description |
|--------|------|-------------|
| **A** | Standard | `LanguageCode=ko-KR` (한국어 고정) |
| **B** | Multi-Language | `IdentifyMultipleLanguages=true` (ko-KR + en-US 자동 감지) |

Evaluator: Claude Sonnet 4.6

Meetings tested: 3

---

## 하나은행 연구개발망 미팅

- Meeting ID: `08417f6d-a7a4-410f-86fa-2d33de73902c`
- Engine A: 33,705 chars
- Engine B: 33,705 chars

# STT 엔진 비교 평가: 하나은행 연구개발망 미팅

---

## 📊 점수표

| 평가 항목 | Engine A (ko-KR 고정) | Engine B (다국어 감지) | 승자 |
|-----------|---------------------|---------------------|------|
| 문맥 연결성 | 5/10 | 5/10 | 동점 |
| 완성도 | 5/10 | 5/10 | 동점 |
| 단어 정확성 | 5/10 | 5/10 | 동점 |
| 전문 용어 | 5/10 | 5/10 | 동점 |
| 화자 구분 | 5/10 | 5/10 | 동점 |
| **종합** | **5/10** | **5/10** | **동점** |

---

## ⚠️ 평가 전 중요 고지

> **두 엔진의 출력이 문자 단위로 완전히 동일합니다.**
> 제공된 텍스트를 정밀 비교한 결과, Engine A와 Engine B의 전사 결과는 **단 한 글자도 차이가 없습니다.**
> 따라서 두 엔진 간의 **차이점 분석은 불가능**하며, 대신 **공통적으로 나타나는 품질 문제**를 분석합니다.

---

## 🔍 상세 분석 (공통 품질 평가)

### 1. 문맥 연결성 — 5/10

전반적으로 구어체 흐름은 유지되나, 오인식된 단어들로 인해 문맥이 심각하게 왜곡되는 구간이 다수 존재합니다.

| 원문 추정 | 전사 결과 | 문제 |
|-----------|-----------|------|
| "로자(Rosa/ROSA)" | "로사" | 일관성은 있으나 정확한 제품명 불명확 |
| "차세대 때문에" | "차선 때문에" | 문맥 단절 — "차선"은 완전히 다른 의미 |
| "피오씨(PoC)" | "피오씨" / "피오시" | **동일 단어를 두 가지로 혼용** → 일관성 파괴 |
| "온프레미스(On-premises)" | "온프레임" | 핵심 IT 개념 오인식으로 문맥 왜곡 |

```
"그 차선 때문에 모든 게 올스톱 돼가지고"
→ 실제 의미: "차세대(프로젝트) 때문에"
→ "차선"으로 인식되어 교통/도로 맥락으로 오해 가능
```

---

### 2. 완성도 — 5/10

발화가 중간에 잘리거나 문장이 미완성으로 끝나는 부분이 반복됩니다.

```
"사실 근데 로사라는 게 그냥 오픈 시프트를 레이"
→ 문장이 완전히 잘려서 끝남 (두 엔진 모두 동일하게 미완성)
```

```
"노사할 때 poc 는 주- 어 목적으로 삼았던 게"
→ "주-" 부분에서 발화 끊김이 그대로 노출
→ 후처리 없이 날것의 전사 상태
```

```
"씨의 주체가 맞아요 맞아요. 하시는 분도 아니고 도 아니에요."
→ "씨의 주체" — 앞뒤 맥락 없이 문장 시작
→ 화자 전환 구간에서 발화 누락 추정
```

---

### 3. 단어 정확성 — 5/10

한국어 구어체 오인식과 고유명사 오인식이 전반에 걸쳐 나타납니다.

| 전사 결과 | 추정 실제 발화 | 오인식 유형 |
|-----------|--------------|------------|
| "라이브자" | "레거시(Legacy)" 또는 "라이브(Live) 시스템" | 고유 개념 오인식 |
| "키우씨" | "KUC" 또는 특정 프로젝트명 | 영문 약어 오인식 |
| "메타나씨" / "메타네티" / "메타티" | "MNO" 또는 특정 파트너사명 | **동일 대상을 3가지로 다르게 인식** |
| "레드에이" / "레드엣" / "레드" | "Red Hat" | **동일 회사를 3가지로 다르게 인식** |
| "로사 pos 성공" | "Rosa PoC 성공" | "PoC"를 "pos"로 오인식 |
| "건진" | "Consulting" 또는 "엔지니어링" | 음절 오인식 |
| "카트라이" | "Canary(카나리)" 또는 "Ctrl+I" | 불명확 |
| "에이플러스" | "AWS" 또는 "A++" | 오인식 |
| "애교로" | "AWS로" 또는 "에코(Echo)로" | 심각한 오인식 |

---

### 4. 전문 용어 — 5/10

AWS/IT 전문 용어 처리에서 심각한 오인식이 다수 발견됩니다.

**올바르게 인식된 용어:**
- `aws` ✅
- `poc` ✅ (단, "pos"로 오인식된 경우도 존재)
- `msk` ✅
- `오픈 시프트(OpenShift)` ✅

**오인식된 전문 용어:**

| 전사 결과 | 실제 추정 용어 | 심각도 |
|-----------|--------------|--------|
| "온프레임" | On-premises (온프레미스) | 🔴 높음 |
| "오픈시코트" | OpenShift (오픈시프트) | 🔴 높음 — 앞에서는 "오픈 시프트"로 정확히 인식했으나 여기서 오인식 |
| "디알 쪽" | DR(Disaster Recovery) | 🟡 중간 |
| "로사 pos" | Rosa PoC | 🔴 높음 |
| "레드에이" / "레드엣" | Red Hat | 🔴 높음 — 동일 발화를 다르게 인식 |
| "아키텍처" | Architecture | 🟢 낮음 (한국어 발음 그대로) |
| "이니셔티브" | Initiative | 🟢 낮음 |

**특히 주목할 문제:**
```
"오픈 시프트" (정확) vs "오픈시코트" (오인식)
→ 동일한 단어를 같은 전사 내에서 다르게 처리
→ 일관성 부재가 후속 텍스트 처리(NLP, 요약 등)에 악영향
```

---

### 5. 화자 구분 — 5/10

두 엔진 모두 화자 레이블(Speaker A/B 등)을 **전혀 제공하지 않았습니다.**

텍스트 내용상 최소 2명 이상의 화자가 교차 발화하고 있음이 명확하나, 구분 없이 연속 텍스트로 출력되었습니다.

**화자 전환 추정 구간 (레이블 없음):**
```
"...이게 아마 처음이 될 거라서 [화자1 종료 추정]
이렇게 되면은 저희는 외부에도... [화자2 시작 추정?]"
```

```
"씨의 주체가 맞아요 맞아요. [화자 전환]
하시는 분도 아니고 도 아니에요." [발화 누락 의심]
```

> 💡 AWS Transcribe의 Speaker Diarization 기능(`ShowSpeakerLabel=true`)이 활성화되지 않은 것으로 보입니다. 이 기능을 활성화하면 화자 구분 점수가 크게 향상될 수 있습니다.

---

## 📋 종합 평가

### 두 엔진이 동일한 결과를 낸 원인 분석

| 가설 | 설명 |
|------|------|
| **동일 오디오 세그먼트 처리** | 해당 미팅 구간에 영어 발화가 거의 없어 다국어 감지 모드가 한국어로만 처리 |
| **영어 비중 임계값 미달** | Engine B의 언어 감지가 영어 전환 트리거를 발동하지 않음 |
| **AWS Transcribe 내부 동작** | 짧은 영어 단어(aws, poc, msk 등)는 한국어 모드에서도 처리 가능하여 차이 미발생 |

### 결론 및 권고사항

```
현재 제공된 샘플에서는 두 엔진의 성능 차이를 판별할 수 없습니다.
```

**한국어+영어 혼용 미팅에 대한 일반적 권고:**

1. **영어 전문 용어가 문장 단위로 등장하는 경우** → Engine B (다국어 감지) 유리
2. **영어가 단어 수준 삽입어로만 등장하는 경우** → Engine A (ko-KR 고정)와 동일하거나 오히려 불안정
3. **현재 가장 시급한 개선 포인트:**
   - `Custom Vocabulary` 등록: `Red Hat`, `OpenShift`, `Rosa`, `On-premises`, `PoC` 등
   - `Speaker Diarization` 활성화
   - `Vocabulary Filter`로 반복 오인식 패턴 교정

> **실질적 차이를 확인하려면** 영어 문장이 명확히 포함된 구간(예: 영어로 발표하는 세션, 영문 용어를 문장으로 설명하는 구간)의 샘플을 추가로 비교 분석하는 것을 권장합니다.

---

## 하나금융기술연구소

- Meeting ID: `84c56bfd-b9f9-40cb-ba88-85d2eddb1fc6`
- Engine A: 37,445 chars
- Engine B: 37,441 chars

# STT 엔진 비교 평가: 하나금융기술연구소 미팅

---

## 점수표

| 평가 항목 | Engine A (ko-KR 고정) | Engine B (다국어 감지) | 승자 |
|-----------|---------------------|---------------------|------|
| 문맥 연결성 | 6/10 | 6.5/10 | B |
| 완성도 | 6/10 | 6.5/10 | B |
| 단어 정확성 | 6/10 | 6.5/10 | B |
| 전문 용어 | 6/10 | 6/10 | 동점 |
| 화자 구분 | 5/10 | 5/10 | 동점 |
| **종합** | **5.8/10** | **6.1/10** | **B** |

> ⚠️ **총평 선요약**: 두 엔진의 차이는 **미미**합니다. 대부분의 오류가 **공통적으로 발생**하며, Engine B가 일부 문장에서 미세하게 더 자연스러운 표현을 보이지만, 결정적 우위는 없습니다.

---

## 상세 분석

### 1. 문맥 연결성 (A: 6/10 | B: 6.5/10)

두 엔진 모두 전반적인 대화 흐름은 유지하나, 특정 구간에서 문맥이 단절됩니다.

#### 🔴 공통 문제
| 구간 | 원문 추정 | 두 엔진 공통 오류 |
|------|----------|----------------|
| GPU 수급 논의 | "GPU가 워낙 없다 보니까" | `지피가 워낙 없다 보니까` — "GPU"를 "지피"로 음차 처리, 문맥상 어색 |
| 클라우드 전략 | "tie에 클라우드" | `tie 에 클라우드` — "TIA(하나금융 IT 자회사)" 미인식, 문맥 단절 |
| 딥다이브 미팅 | "딥다이브 미팅" | `디따 미팅` — 완전히 다른 단어로 인식, 문맥 혼란 |

#### 🟡 Engine A vs Engine B 차이점

**Engine A 문제:**
```
"거든요. 그러다 보니까 그런 기능까지 빨리 adption 을 해가지고서"
→ 앞 문장 끝이 "시간이 걸리거든요." 로 끝나며 문장 연결이 다소 단절됨
```

**Engine B 개선:**
```
"시간이 걸리는 거죠. 그 그 시간을 이제..."
→ "걸리거든요" → "걸리는 거죠"로 변환, 구어체 흐름이 더 자연스러움
```

**Engine A 추가 문제:**
```
"티에 클라를 쓰고 있는데 티아 왔다 갔다..."
→ 앞에 "요런 그런 질문들이..." 단락과 연결이 갑작스러움
```

**Engine B:**
```
"클라를 쓰고 있는데 티아 왔다 갔다..."
→ "티에" 제거로 오히려 더 간결하나, 여전히 문맥 연결 불완전
```

---

### 2. 완성도 (A: 6/10 | B: 6.5/10)

#### 🔴 공통 누락/잘림
```
두 엔진 모두 마지막 문장이 동일하게 잘림:
"그거는 저희가 메인으로 쓴..." (문장 미완성)
→ 오디오 길이 제한 또는 전사 중단으로 추정
```

```
"저희가 를 하고 있거든요"
→ 두 엔진 모두 동사/목적어 누락. 원문 추정: "저희가 [블로그 포스팅/공유] 를 하고 있거든요"
```

```
"이런 거pu 나 그다음에"
→ 두 엔진 모두 동일하게 "GPU나" 를 "거pu 나"로 분절 오인식
```

#### 🟡 Engine A만의 누락
```
Engine A: "티에 클라를 쓰고 있는데"
Engine B: "클라를 쓰고 있는데"

→ Engine A는 "티에"라는 불필요한 음절을 삽입하여 오히려 완성도 저하
  (원문 추정: "[TIA] 클라우드를 쓰고 있는데")
```

#### 🟡 Engine B만의 누락
```
Engine A: "ti aws 를 고려를 하고 있나요?"
Engine B: "ti aws 를 고려를 하고 있나요?" (동일)

→ 두 엔진 모두 "TIA에서 AWS를 고려하고 있나요?"를 제대로 처리 못함
  그러나 Engine A 앞 문장에 "티에 클라를 쓰고 있는데"가 추가로 있어
  오히려 Engine A가 더 많은 오삽입 발생
```

---

### 3. 단어 정확성 (A: 6/10 | B: 6.5/10)

#### 🔴 두 엔진 공통 오인식 단어

| 오인식 단어 | 추정 원문 | 비고 |
|------------|----------|------|
| `지피` | GPU | 영어 약어 한글 음차 실패 |
| `디따 미팅` | 딥다이브 미팅 | 심각한 오인식 |
| `아이브자` | AI부자? / Advisor? | 문맥 불명확 |
| `패션` | 세션(session) | `딥한 패션` → `딥한 세션` |
| `adption` | adoption | 영어 철자 오류 |
| `fint튜닝` | fine-tuning | 혼합 표기 오류 |
| `복용` | 소요 / 걸려요 | `1~2년 복용` → `1~2년 소요` |
| `업테리치` | 업스테이지(Upstage)? | 기업명 오인식 |
| `twelves` | Twelve Labs? | 기업명 오인식 |
| `그래프랙` | GraphRAG? | 기술 용어 오인식 |
| `이안 님` | Ian? / 이안? | 인명 불확실 |
| `윤변` | 윤 변호사? / 인명? | 문맥 불명확 |

#### 🟡 Engine A만의 오류
```
"거든요." → Engine A: "시간이 걸리거든요."
           Engine B: "시간이 걸리는 거죠."
→ 구어체 표현에서 B가 더 자연스러운 어미 선택
```

```
Engine A: "티에 클라를 쓰고 있는데"
Engine B: "클라를 쓰고 있는데"
→ Engine A가 "티에"라는 불필요 음절 삽입 (오삽입 오류)
```

```
Engine A: "ti에도 그 그 퍼블릭 클라우드"
Engine B: "티아에도 그 그 퍼블릭 클라우드"
→ Engine B가 "TIA"를 "티아"로 일관되게 처리 (더 정확)
```

#### 🟡 Engine B만의 오류
- 이 구간에서는 Engine B의 독자적 오류가 Engine A보다 적음
- 대부분 Engine A 오류를 그대로 공유하거나 미세하게 개선

---

### 4. 전문 용어 (A: 6/10 | B: 6/10 — 동점)

#### 🔴 두 엔진 공통 전문 용어 오류

| 오인식 | 추정 정확 용어 | 심각도 |
|--------|--------------|--------|
| `cg 메이커` | SageMaker | ⭐⭐⭐ 심각 |
| `세일즈 메이크` / `세이즈 메이크` | SageMaker | ⭐⭐⭐ 심각 |
| `ksd` | EKS | ⭐⭐⭐ 심각 |
| `이 케이에스` / `이케s` | EKS | ⭐⭐ 중간 (음차는 맞으나 표기 불일치) |
| `세이지 메이커 하이퍼 파드` | SageMaker HyperPod | ⭐ 양호 (의미 전달됨) |
| `dra 어웨어 라우팅` | DRA-aware routing? | ⭐⭐ 원어 불명확 |
| `kb 캐시 어웨어 라우팅` | KV Cache-aware routing | ⭐⭐ 중간 |
| `로라` | LoRA | ⭐ 양호 |
| `puc` | POC | ⭐⭐ 중간 |
| `aia` | AI? / AIA? | ⭐⭐ 문맥 불명확 |
| `세일즈 메이크 유니파이드 스튜디오` | SageMaker Unified Studio | ⭐⭐ 중간 |

#### 🟡 미세한 차이점

```
"SageMaker" 처리:
- Engine A: "세일즈 메이크", "cg 메이커", "세이지 메이커"  (3가지 다른 표기 혼재)
- Engine B: "세일즈 메이크", "cg 메이커", "세이지 메이커"  (동일하게 혼재)
→ 두 엔진 모두 동일 서비스명을 일관되게 처리하지 못함 — 동점
```

```
"EKS" 처리:
- Engine A: "이 케이에스", "이케s", "eks", "ksd" 혼재
- Engine B: "이 케이에스", "이케s", "eks", "ksd" 혼재
→ 완전 동일 — 동점
```

```
"온프레미스" 처리:
- 두 엔진 모두: "온프레을" → "온프레미스(on-premises)" 미완성 처리
→ 동점
```

---

### 5. 화자 구분 (A: 5/10 | B: 5/10 — 동점)

#### 🔴 공통 문제: 화자 레이블 없음
```
두 엔진 모두 화자 분리(Speaker Diarization) 태그가 
출력 텍스트에 포함되어 있지 않습니다.

실제 대화에는 최소 3명 이상의 화자가 존재하는 것으로 추정:
- AWS 담당자 (세일즈/SA)
- 하나금융기술연구소 NLP 담당자
- 하나금융기술연구소 플랫폼 담당자
```

#### 🟡 화자 전환 추정 가능 구간 (두 엔진 동일)
```
[화자 전환 추정 지점들 — 두 엔진 모두 동일하게 처리]

1. "네 오픈 모델을 가지고 와서 사전 학습하고..."
   → 질문자(AWS)에서 답변자(하나금융)로 전환 추정
   → 두 엔진 모두 구분 없이 연속 텍스트로 처리

2. "보통 근데 근데 예전에 이제 저한테 처음에 와가지고서..."
   → AWS SA의 기술 설명 시작으로 추정
   → 두 엔진 모두 구분 없음

3. "어 네네. 티아에도 그 그 퍼블릭 클라우드..."
   → 하나금융 답변자로 전환 추정
   → 두 엔진 모두 구분 없음
```

> 💡 **참고**: AWS Transcribe의 화자 분리 기능(`ShowSpeakerLabel=true`)이 활성화되지 않은 것으로 보입니다. 두 엔진 모두 동일 조건이므로 동점 처리.

---

## 종합 평가

### 결론: **Engine B (다국어 자동 감지) 미세 우위** — 단, 실질적 차이 매우 제한적

```
Engine A 최종: 5.8/10
Engine B 최종: 6.1/10
차이: 0.3점 (매우 근소)
```

### Engine B가 나은 이유 (구체적 근거)

| 항목 | 근거 |
|------|------|
| **오삽입 오류 적음** | "티에 클라를 쓰고 있는데" 같은 불필요 음절 삽입이 B에서 제거됨 |
| **고유명사 일관성** | "TIA"를 "티아"로 일관 처리 (A는 "티에"/"ti" 혼재) |
| **어미 자연스러움** | "걸리거든요" → "걸리는 거죠" 등 구어체 처리 미세 개선 |

### 그러나 두 엔진 모두의 근본적 한계

> 이 미팅의 STT 품질 문제는 **엔진 선택보다 더 근

---

## 하나금융기술연구소(Mobile)

- Meeting ID: `6efb247f-d422-4f7c-aa96-e6f875e55190`
- Engine A: 36,729 chars
- Engine B: 36,729 chars

# STT 엔진 비교 평가 보고서
## 하나금융기술연구소(Mobile) 미팅

---

## 📊 점수표

| 평가 항목 | Engine A (ko-KR 고정) | Engine B (다국어 감지) | 승자 |
|-----------|---------------------|---------------------|------|
| 문맥 연결성 | 6/10 | 6/10 | 동점 |
| 완성도 | 6/10 | 6/10 | 동점 |
| 단어 정확성 | 6/10 | 6/10 | 동점 |
| 전문 용어 | 5/10 | 5/10 | 동점 |
| 화자 구분 | 5/10 | 5/10 | 동점 |
| **종합** | **5.6/10** | **5.6/10** | **동점** |

---

## 🔍 상세 분석

### 핵심 발견: 두 엔진의 출력이 **사실상 동일**

가장 중요한 발견은 제공된 텍스트 범위 내에서 **Engine A와 Engine B의 출력이 단어 하나 차이 없이 완전히 동일**하다는 점입니다.

> 다국어 자동 감지(IdentifyMultipleLanguages) 모드가 의미 있는 차이를 만들어내지 **못했습니다.**

이는 다음을 시사합니다:
- 이 미팅의 영어 혼용 비율이 엔진 전환을 트리거할 만큼 충분하지 않았거나
- 영어 발화가 짧은 단어/구문 수준에 머물러 언어 전환 감지가 작동하지 않았을 가능성이 높습니다

따라서 아래 분석은 **두 엔진 공통의 문제점**을 중심으로 진행합니다.

---

### 1. 문맥 연결성 (6/10 공통)

**문제점: 한국어 음성 인식 오류로 인한 문맥 단절**

| 원문 추정 | 인식 결과 | 문제 |
|-----------|-----------|------|
| "집중적인 짧은 미팅" | `"집 짧은 미팅"` | '집중적인' 탈락으로 문맥 불명확 |
| "딥한 세션(session)" | `"딥한 패션"` | 완전히 다른 단어로 오인식 |
| "프롬 스크래치(from scratch)" | `"프랑스 크래치나"` | 영어 발음을 한국어로 오인식하여 문맥 단절 |

**특이 사항:** `"프랑스 크래치나"` 오류는 두 엔진 모두 동일하게 발생 → Engine B의 다국어 감지가 "from scratch"를 영어로 인식하지 못한 명백한 실패 사례

---

### 2. 완성도 (6/10 공통)

**문제점: 말 잘림 및 단어 누락**

| 위치 | 인식 결과 | 문제 |
|------|-----------|------|
| 중반부 | `"구매하시- 뭐 하시더라도"` | 발화 중단 그대로 노출, 후처리 없음 |
| 초반부 | `"저희도 뭘 수 있을지"` | '뭘 할 수 있을지'에서 '할' 누락 |
| 중반부 | `"후계생하실 때"` | '후계생' → '회계 산정' 또는 '예산 책정' 추정, 심각한 오인식 |

---

### 3. 단어 정확성 (6/10 공통)

**공통 오인식 목록**

| 추정 원문 | 인식 결과 | 심각도 |
|-----------|-----------|--------|
| `AWS` | `에이더블유에스` | 낮음 (발음 그대로) |
| `세션(session)` | `패션` | **높음** |
| `from scratch` | `프랑스 크래치나` | **높음** |
| `adoption` | `aution` | 중간 |
| `private cloud` | `플라이 풀 클라이드` | **높음** |
| `연간 계획` | `연애 계획` | **높음** (의미 왜곡) |
| `GPU` | `지피유` | 낮음 |
| `SageMaker Unified Studio` | `세일즈 메이크 유니파이 스튜디오` | **높음** |

**주목할 오류:**
- `"연애 계획"` — '연간 계획' 또는 '연도 계획'의 오인식으로, 비즈니스 문서에서 의미를 완전히 왜곡
- `"세일즈 메이크"` — SageMaker를 완전히 다른 단어로 인식

---

### 4. 전문 용어 (5/10 공통)

**AWS/IT 전문 용어 인식 현황**

| 용어 | 인식 결과 | 평가 |
|------|-----------|------|
| `EKS` | `이케이에스` / `이케s` | 혼재 (비일관성) |
| `SageMaker` | `세이지 메이커` / `세일즈 메이크` | **혼재** |
| `SageMaker HyperPod` | `세이지 메이커 하이퍼 파드` | 양호 |
| `LoRA` | `로라` | 양호 |
| `RAG` | `dra` | 오인식 |
| `KV Cache` | `kb 캐쉬` | 오인식 |
| `Kubernetes` | `쿠버네티스` | 양호 |
| `on-premise` | `온 프레임` | 오인식 |
| `SageMaker Unified Studio` | `세일즈 메이크 유니파이 스튜디오` | **심각** |
| `Upstage` | `업스테이치` | 오인식 |
| `12Labs` | `투웰브 앱스` | 오인식 |

**Engine B 기대 효과 미달성:**
다국어 감지 모드였음에도 `EKS`, `SageMaker`, `RAG`, `LoRA` 등 영어 약어/고유명사를 영어 텍스트로 출력하지 못하고 한국어 발음 표기로만 처리 → 다국어 모드의 핵심 이점이 발현되지 않음

---

### 5. 화자 구분 (5/10 공통)

**문제점: 화자 전환 마커 부재**

```
...좋을 것 같아요 네 자료는 어떤 자료를죠? 저희한테 오늘 회의자료 주신거 아니었어요?
삼성 화재의 케이스를 말씀을 드리면 될까요?
```

- 위 구간에서 최소 2~3명의 화자가 교차하고 있으나 구분 없이 연속 텍스트로 출력
- `[화자 A]`, `[화자 B]` 등의 Speaker Diarization이 적용되지 않음
- 두 엔진 모두 동일한 한계 보유

---

## 📋 종합 평가

### 결론: **두 엔진 간 유의미한 차이 없음 — 현재 설정으로는 동점**

```
┌─────────────────────────────────────────────────────┐
│  Engine A (ko-KR 고정) ≈ Engine B (다국어 감지)      │
│                                                     │
│  다국어 감지 모드의 추가 비용/복잡성 대비             │
│  실질적 품질 향상 없음                               │
└─────────────────────────────────────────────────────┘
```

### 한국어+영어 혼용 미팅에 대한 권고사항

**현재 두 엔진 모두 이 미팅 유형에 최적화되어 있지 않습니다.**
근본 원인은 엔진 선택보다 **아래 설정 부재**에 있습니다:

| 권고 설정 | 기대 효과 |
|-----------|-----------|
| **Custom Vocabulary** 적용 | EKS→EKS, SageMaker→SageMaker 정확 인식 |
| **Speaker Diarization** 활성화 | 화자 구분 해결 |
| **Vocabulary Filter** | 오인식 단어 후처리 |
| **Custom Language Model** | 금융+AWS 도메인 특화 학습 |

### 엔진 선택 가이드

```
영어 발화 비율 < 20%  →  Engine A (ko-KR 고정) 권장
                         (이 미팅이 해당)

영어 발화 비율 > 30%  →  Engine B (다국어 감지) 권장
또는 영어 문장 단위    →  단, Custom Vocabulary 병행 필수
발화가 빈번한 경우
```

> **최종 권고:** 이 미팅처럼 한국어가 주언어이고 영어가 단어/구문 수준으로만 혼용되는 경우, **Engine A(ko-KR 고정) + Custom Vocabulary** 조합이 비용 효율적이며 충분한 성능을 제공합니다. Engine B의 다국어 감지는 영어 문장이 빈번하게 등장하는 미팅에서 진가를 발휘합니다.

---

