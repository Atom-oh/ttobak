[project]
name = "research-agent"
version = "0.1.0"
description = "Ttobak Deep Research Agent — AgentCore Runtime container"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "strands-agents>=1.36.0",
    "boto3>=1.34.0",
]
